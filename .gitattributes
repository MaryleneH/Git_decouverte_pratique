# Les mini-films (assets/video/*.mp4, ~10 Mo chacun après ré-encodage web)
# sont des blobs Git ordinaires : pas de LFS, donc aucun quota de bande
# passante consommé au clone ni au déploiement.
*.parquet filter=lfs diff=lfs merge=lfs -text
